@model  PagedList.IPagedList<ICStars2_0.Model.GEO133>

@using PagedList.Mvc;
<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />
<link href="~/Content/Css/MvpHome.css" rel="stylesheet" type="text/css" />
@{
    ViewBag.Title = "Forms List";
    Layout = "~/Areas/SHTracker/Views/Shared/_Layout.cshtml";
}
<h2>List</h2>
<h2>Index</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    @*<tr>

        <th>
            @Html.DisplayNameFor(model => model.DpuId)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.buildingAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.blockNumber)
        </th>
      
        <th></th>
    </tr>*@

    @foreach (var item in Model)
    {
        <tr class=" bar">
            <td>


                @Html.DisplayFor(modelItem => item.DpuId)
            </td>


            <td>
                @Html.DisplayFor(modelItem => item.buildingAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.blockNumber)
            </td>
         
            <td>
              @*  @Html.ActionLink("Edit", "Edit", new { id = item.OrgId }) |*@
                @Html.ActionLink("Details", "Details", new { id = item.Id }) 
              @*  @Html.ActionLink("Delete", "Delete", new { id = item.OrgId })*@
            </td>
        </tr>
    }

</table>
<p style="width:300px;color:black ; font-size:14px;Padding:3em;">
    Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
    @Html.PagedListPager(Model, page => Url.Action("List",
    new { page, sortOrder = ViewBag.CurrentSort, currentFilter = ViewBag.CurrentFilter }))
</p>
