using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Commengdpu.Models;

namespace Commengdpu.DBLayer
{
    public interface IDbUser2Role
    {
        int Insert(IEnumerable<User2Role> user2Roles);
        int Update(IEnumerable<User2Role> user2Roles);
        int Delete(IEnumerable<User2Role> user2Roles);
        int DeleteByUserId(int userId);
        IEnumerable<User2Role> List(int userId);
    }
}
